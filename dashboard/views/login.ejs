<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion - Coffey.ai</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex flex-col items-center justify-center">
    <div class="max-w-md w-full p-6 bg-gray-800 rounded-lg shadow-lg text-center">
        <h1 class="text-2xl font-bold mb-4">🔐 Connexion Coffey.ai</h1>
        <% if (error) { %>
            <p class="text-red-500 mb-4"><%= error %></p>
        <% } %>
        <form method="POST" action="/login" class="flex flex-col space-y-4">
            <input type="password" name="password" placeholder="Mot de passe" required
                   class="p-2 rounded bg-gray-700 border border-gray-600 text-white">
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 rounded">Se connecter</button>
        </form>
    </div>
</body>
</html>
